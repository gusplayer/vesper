import { Screen, Text } from '../../design/components';

/** Placeholder — replaced by the screen team. */
export default function PlaceholderScreen() {
  return (
    <Screen>
      <Text variant="heading">Ayuda</Text>
      <Text tone="secondary">en construcción</Text>
    </Screen>
  );
}
