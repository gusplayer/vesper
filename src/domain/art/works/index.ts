import type { Artwork } from '../types';
import { placeholder } from './placeholder';

/** Registered artworks. Add a file per work and list it here. */
export const WORKS: ReadonlyArray<Artwork> = [placeholder];
