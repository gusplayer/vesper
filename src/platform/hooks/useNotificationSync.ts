/**
 * Keeps the platform in step with the stores by subscribing to them from outside:
 * the stores never import the platform, so the app builds where the platform is
 * missing. Filled in by its team.
 */
export function useNotificationSync(): void {
  // No-op until integrated.
}
